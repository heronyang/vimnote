{
  "name": "vimnote",
  "version": "1.0.0",
  "description": "Take note with Vim.",
  "author": "Heron Yang <heron.yang.tw@gmail.com>",
  "main": "server.js",
  "scripts": {
    "db_init": "psql postgres < db_reset.sql && psql postgres -U vimnote_server < db_init.sql",
    "test": "mocha",
    "precommit": "lint-staged"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^7.18.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "husky": "^4.2.3",
    "lint-staged": "^10.0.8",
    "mocha": "^7.1.1",
    "prettier": "2.0.1",
    "pretty-quick": "^2.0.1",
    "request": "^2.88.2",
    "standard": "^14.3.3"
  },
  "lint-staged": {
    "*.js": [
      "./node_modules/prettier/bin-prettier.js --write",
      "git add"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  }
}
